"""Feature engineering package.

Collaborative indices (`user_idx` / `movie_idx`) are built in
``scripts/feature_engineering.py``. Optional topic models (BERTopic) live
behind the ``topics`` extra in ``pyproject.toml`` and are not required by
the Tech Challenge PDF.
"""
